<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ex5_1_1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"> <!-- 定义APP主题风格 -->
        <activity android:name=".Main1Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity" />
        <activity android:name=".login" />
        <activity android:name=".MaindengluActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ContentActivity" />
        <activity android:name=".Mycomments" />
        <activity android:name=".Feedback" />
        <activity android:name=".TijiaofankuiActivity"></activity>
        <activity android:name=".Scores"></activity>
    </application>

</manifest>